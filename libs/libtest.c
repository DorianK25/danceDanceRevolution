#include "lib.h"

int main(){
   /* writeScore(30);
    writeScore(70);
    writeScore(2);
    writeScore(20);*/
    //topScore();
   // afficherNiveau(10);
    //getSaisie();
   // writeRoom("Salle1", getpid());
    readRoom();
    return 0;
}